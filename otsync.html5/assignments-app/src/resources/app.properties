# Defaults as follows:
#	 * Display name -> app name
# * Release number -> 1
# * Display version -> Release number (as a string)
# * Minimum client version -> 0
# * type -> app (the other option is service)
# * description -> Display name
displayName=Assignments
description=List the current user's assignments.
version=16.0.1
minimumClientVersion=0
type=app
awgPlatformVersion=16
displayName.fr=Tâches assignées
